@model IEnumerable<DeBrabander.Models.PostalCode>

@{
    ViewBag.Title = "Index";
}




<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostalCodeNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Town)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostalCodeNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Town)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostalCodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostalCodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostalCodeId })
        </td>
    </tr>
}

</table>
